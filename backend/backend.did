service : {
  deleteFile: (text) -> (bool);
  getFileChunk: (text, nat) -> (opt blob);
  getFiles: () -> (vec record {
                         name: text;
                         size: nat;
                       });
  getTotalChunks: (text) -> (nat);
  isRegistered: () -> (bool);
  registerUser: () -> ();
  uploadFileChunk: (text, blob, nat, nat) -> ();
}
